require 'spec_helper'

describe User do
  it "is valid with a nickname, avatar, token, os, os_version"
  it "is invalid without a nickname"
  it "is invalid without a avatar"
  it "is invalid without a device token"
  it "is invalid without a device os"
  it "is invalid without a device os version"
 	it "is invalid with a duplicate device token"
end
